# GOATGuard Server — dependencias completas
# API + Pipeline + Detection

# API Framework
fastapi==0.135.1
uvicorn==0.42.0
pydantic>=2.0

# Base de datos
sqlalchemy>=2.0
psycopg2-binary==2.9.11
alembic>=1.13,<2.0

# Autenticación 2FA [RF-13]
bcrypt==5.0.0
PyJWT==2.12.1
pyotp>=2.9
qrcode[pil]>=7.4
cryptography>=42
httpx>=0.27
slowapi>=0.1.9

# Configuración
pyyaml>=6.0

# Captura y análisis de red (pipeline)
scapy>=2.5
pythonping>=1.1
mac-vendor-lookup>=0.1

# WebSocket
websockets>=11.0

# Push notifications (Firebase Cloud Messaging)
firebase-admin>=6.5